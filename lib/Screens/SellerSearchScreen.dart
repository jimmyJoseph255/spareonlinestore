import 'package:flutter/material.dart';

class SellerSearchScreen extends StatelessWidget {
  const SellerSearchScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Seller Search'),
      ),
    );
  }
}
